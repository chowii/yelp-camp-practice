const CampgroundModel = require('../campground/data/campgroundModel')
const ApiError = require("../error/ApiError");

module.exports.isReviewAuthor = async (req, res, next) => {
   // TODO implement logic for review author
}
